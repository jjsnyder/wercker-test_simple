box:
  id: openjdk

build:
  steps:
    - script:
        name: step 1
        code: |
          ls
          which curl

  after-steps:
    - slack-notifier:
      url:  $SLACK_URL
      channel: $SLACK_CHANNEL
      username: $SLACK_USERNAME

jj:
  steps:
    - script:
        name: step 1
        code: |
          ls



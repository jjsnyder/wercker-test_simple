box:
  id: openjdk

build:
  steps:
    - script:
        name: step 1
        code: |
          ls

  after-steps:
    - slack-notifier:
      url:  $SLACK_URL
      channel: $SLACK_CHANNEL
      username: $SLACK_USERNAME

deploy:
  steps:
    - script:
        name: step 1
        code: |
          ls

  after-steps:
    - slack-notifier:
      url:  $SLACK_URL
      channel: $SLACK_CHANNEL
      username: $SLACK_USERNAME


test_it:
  steps:
    - script:
        name: step 1
        code: |
          ls

  after-steps:
    - slack-notifier:
      url:  $SLACK_URL
      channel: $SLACK_CHANNEL
      username: $SLACK_USERNAME



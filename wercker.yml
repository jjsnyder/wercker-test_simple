box:
  id: openjdk

build:
  steps:
    - script:
        name: step 1
        code: |
          ls

  after-steps:
    - slack-notifier:
      url:  $SLACK_URL

deploy:
  steps:
    - script:
        name: step 1
        code: |
          ls

  after-steps:
    - slack-notifier:
      url:  $SLACK_URL
      username: $SLACK_USERNAME


test_it:
  steps:
    - script:
        name: step 1
        code: |
          ls

  after-steps:
    - slack-notifier:
      url:  $SLACK_URL
      username: $SLACK_USERNAME


